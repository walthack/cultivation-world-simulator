# v1.9 Spec — L4 剧情自主演化（Narrative Director）

## Status
Draft（御主「按预定/按推荐」启 design — 2026-06-16,Hassan)。Prereq: v1.8（L3）✅ on main（@da1087c0,pushed）。
**Design Q 按下表收敛值锁定,待 dispatch codex 二审 vet。未实现。** 这是总目标的**灵魂一跳**:从「世界模拟器 + 剧本皮肤」跃迁到「剧本驱动的故事引擎」。

> 起草依据:**v1.9 spike**(`docs/specs/spike-narrative-director-report.md` —— 架构 §1-3 proposal boundary / 分层记忆 / Director-Simulator 协调 + §5 五大风险;minimax M2.7 4 轮滚动**已实证**连贯/顺剧本/跨轮记忆),以及 **v1.8 L3 已落地基建**(下文「继承」)。

---

## 边界（锁死,先于一切 —— 注意:这是对 v1.8 的【反转】)

> **v1.9 = LLM 自主生成符合走向的剧情,超越预设锚点。半 AI → 真 narrative AI。**

| | v1.8 (L3) | v1.9 (L4) |
|---|---|---|
| LLM 角色 | 锚点间**填充**(flavor) | 锚点间/外**自主演化走向**(director) |
| 走向 | scripted 锚点定死,beat 不可改 | **锚点 = 必达里程碑;director 在其间/外自主生成方向性事件** |
| beat 机制权限 | 仅 relation_delta(且写集合非干涉) | **受 backbone 约束的、更宽但有界的 effect 词汇**(可改走向) |
| 核心不变量 | beat 永不改变哪些事件触发 | **director 不得违反 backbone 禁忌、不得使任一 mandatory 锚点不可达** |

**v1.8 的核心不变量(beat 非干涉)在 v1.9 被【有意解除】** —— director 就是要碰走向。所以安全契约从「**非干涉**」变为「**backbone 约束 + 有界权限 + mandatory 锚点仍可达**」。

**硬不变量(架构级,不可越界)**:
1. **backbone 至上(不可变宪法)**:每个 scenario 声明一份 immutable backbone —— thesis/themes/**prohibited outcomes**/关键派系角色/**mandatory 锚点**(spike §2.1,版本化 + hash)。director 生成的任何事件**不得违反 prohibited outcomes**,**不得使任一 mandatory 锚点变得不可达**。这是 L4 取代 L3「非干涉」的新守门线。
2. **Simulator 是唯一 tick 主人 + 唯一 mutation 协调者**(spike §3):director 是**异步 planner**,只能**提议**,不能推进时间、不能直接写 world。提议经 schema/ID/condition/backbone/可达性校验后,走与所有世界改动**同一条确定性 mutation lane** 应用。director **不接收可变 world**,只收不可变快照,返回受校验数据(继承 v1.8 snapshot-only)。
3. **有界权限(authority boundary —— L4 最关键决策,spike §5.1)**:director 提议的事件能 mutate 什么?比 L3(仅 relation_delta)宽,但**仍是白名单**:**禁止**违反 backbone 的不可逆操作(复活死者、推翻已定派系归属、违反 prohibited outcome);**player-exclusive / 纯机制结算**(如玩家关键抉择、战斗判定)**保留**,director 不得代行。待定具体词汇(Q2)。
4. **优先级仲裁(spike §3)**:`hard_anchor(mandatory) > player_pending_choice > accepted_director_main > mechanical_major > director_side > ambient_random`。冲突由优先级解决,不静默覆盖已应用机制。mandatory 锚点永远赢。
5. **长程连贯(plot ledger,spike §2)**:完整分层记忆 —— immutable backbone + 权威世界快照 + **plot ledger**(unresolved threads / promises / secrets / 已完成锚点 / 已废分支 / 因果事实,每条带稳定 ID + source event + 时间 + 失效状态)+ rolling digest(近期逐字 + 远期按 arc 摘要,**不可逆事实单独保全**防摘要复活死者)+ director plan state。这是 L4 区别于 L3「有界 gap 记忆」的核心新建。
6. **降级即纯锚点骨架**:无 LLM key / 失败 / 超时 → director 不提议,**mandatory 锚点照常按时触发**,游戏退化为 v1.6 scripted 骨架,完整可玩(继承 v1.8 降级)。

> v2 才把「自动从小说长出 scenario」接上;v1.9 专注「给定 backbone,LLM 自主演故事」。

### 待锁的最关键决策（M1 之前必先定)
- **Q2 director 权限词汇**(对应 v1.8 的「文本存哪」/L3 的「beat 能 mutate 什么」):L4 必然比 L3 宽(否则没有「自主演化」),但宽到哪?候选见 Q2。
- **可达性守门怎么做**:v1.8 用「beat 不改任何未来事件 condition」的强非干涉。L4 director **要**改走向 → 不能用 L3 那套。改用 **mandatory 锚点的前置可满足性 + 定时重放**:director 应用后,**所有 mandatory 锚点仍存在一条可达路径**(否则拒绝该提议)。这是 L4 守门的根本难点。

---

---

## 决策（御主「按预定」启 design → codex 二审后收敛,2026-06-16,下表锁定)

| # | 决策 | 收敛值（codex 二审后) |
|---|---|---|
| **架构** | director 接口 | **snapshot-only**(继承 v1.8):收不可变快照,返回受校验 proposal 数据;不接收可变 world |
| **架构** | 机制如何应用 | **专属确定性 applier + 独立 director command schema**,不走 `apply_effects`(rollback 不安全 + mod 不可控) |
| **守门** | 取代 L3 非干涉 | **backbone 约束 + 确定性前向重放:director-on/off 重放到 pending mandatory,完整 mandatory ID 集 + 触发月 + 禁忌事实一致**(codex DISSENT 采纳,见 Q3) |
| Q1 | backbone schema | scenario `backbone` 段:文本 thesis(给 LLM/critic)+ **结构化硬 gate**:`prohibited_predicates`(机器校验)+ protected entity/faction IDs + **不可逆事实集** + 结构化 `mandatory_anchors` + version/hash。**文本 prohibited 不承担硬禁忌**(codex 修正) |
| Q2 | **director 权限词汇(最关键)** | (b) 软 + 受限硬,但 **不复用 `world_event_trigger`**(它直接写 `triggered_event_ids`,能把 mandatory 伪造成已触发,`effect_applier.py:165`)。**新建 director command schema**:scoped flag/var、bounded relation、introduce_minor_npc、emit_director_event、local_crisis。**硬权限到前向重放 gate 过后才开**(M0 先 bootstrap,见 M0) |
| Q3 | 可达性守门 | **DISSENT 采纳 → 改真前向重放**:保守"只护 mandatory 前置读集合"**不够** —— v1.9 解除非干涉后会重开 v1.8 已修的 indirect-blocks/branch-condition/mod-effect 三桥。守门 = **deterministic forward replay**(同初始态/RNG,director-on/off 重放至 pending mandatory,断言全达 + 禁忌不变)+ 三类 negative fixture。L3 的"护读集合"不再适用 |
| Q4 | plot ledger | CONCUR-with-escalation:threads + 完成锚点 + **不可逆事实** + rolling digest(不可逆事实单独保全)。**不可逆事实集必须在任何受限硬事件启用【之前】就位**(不能拖到 M2 后)——否则复活/归属回滚/摘要复活无法 deterministic gate(codex 升级)。plan state + secrets 后置 |
| Q5 | 持久化/replay | director-turn + backbone hash + locale 入键;**保存当时 backbone snapshot/hash + normalize 事件/ledger delta**;reload 时 backbone hash 不符 → **冻结旧 backbone 随存档重放 或 拒绝/降级 director replay**,绝不用新 backbone 解释旧 ledger(codex 加固) |
| Q6 | 调度/优先级 | **新建真实中心调度仲裁**(非仅 spec 文字):现 dispatcher 是 exact-month + author-order 单趟扫(`event_dispatcher.py:49`)。需 mandatory-first + 同月 mandatory/player/director/mechanical/ambient 的排序 + 冲突压制语义。优先级 `mandatory > player_choice > director_main > mechanical > director_side > ambient` |
| Q7 | 一致性 critic | **辅助手段**:确定性 schema/backbone/可达 gate 先过,critic 仅做漂移/重复/软矛盾提示,失败不阻塞纯锚点降级 |
| Q8 | 长程漂移 benchmark | **L4 merge gate(非 nice-to-have)**:专列 M5,跑 N 年仿真,断言 mandatory 全达 / 无复活 / 无违禁 / beat 多样达阈。spike 仅证 4 轮短程,不能证几十年 |
| Q9 | 成本/延迟 | 同步有界批量起步;异步 plan-ahead 留 M6,**且 plan-ahead 必带 snapshot revision/precondition retry**(spike §3 stale proposal 须拒) |
| Q10 | prompt 注入/mock | 继承 v1.8 `_clip`+fenced + 可注入 generator;fixture **必须覆盖**:违 prohibited / 违不可逆 / 违 player-exclusive / 间接饿死 / bad ledger delta / backbone hash mismatch |

**实施起点 = M0(codex 修正版,见下方 Milestones M0)**:backbone 结构化 schema + **非空过 proposal boundary** + **前向重放 harness**;**Q2 硬权限在重放 gate 过之前不开**(M0 先 bootstrap:director 只写"无任何 pending timeline/机制读取"的 scoped facts,证 proposal boundary/schema 不空过,但**不宣称 L4 自主演化已成立**)。下方 Design Q 全文保留作决策依据。

## Context / 阶梯定位 + 继承 v1.8

- roadmap **L4**(`roadmap-to-narrative-goal.md` line 42-43):LLM 理解剧本脉络 + 世界状态,自主生成符合走向的剧情(超越预设锚点)。**真 narrative AI 攻坚 —— ③ 达成,总目标灵魂落地。** 设计挑战:剧情连贯/长程记忆、剧本约束 vs 涌现、director 与模拟引擎协调、成本/延迟。
- **继承 v1.8 L3 基建(不从零造)**:
  - **snapshot-only generator 接口** + 严格超时 + 降级(`make_transition_generator` 模式)→ director 同构。
  - **专属确定性 applier + 命令白名单 + write-set 提取**(`narrative_transition`)→ L4 扩展白名单 + 校验,复用 applier 思路(**不走 `apply_effects`**,理由同 L3:rollback 不安全 + mod effect 不可控)。
  - **冻结缓存 + 确定性 replay**(`transition_cache`,gap+locale 键)→ director 提议的固化/replay 同构(键需扩,见 Q5)。
  - **审计 ledger**(`transition_ledger`,state 兄弟,持久)→ 升级为 plot ledger(带因果/失效)。
  - **anchor 概念 + load 时 timed 可达校验**(`_validate_anchor_reachability`)→ mandatory 锚点复用,可达校验升级为「director 应用后仍可达」运行时校验。
  - **render-only narration 通道 + chronicle exclude_story**(v1.7/v1.8)→ director 叙事文本同走 narration,不污染机制/记忆。
  - **proposal boundary 蓝图**(spike §1):`snapshot → director proposal → schema/ID/condition/backbone 校验 → 确定性 command 应用 → Event → ctx.events`。
- **真正新建**(L3 都没有的):① immutable **backbone**(thesis/prohibited/mandatory)+ 版本化;② **plot ledger**(长程因果记忆 + 失效);③ **优先级仲裁 + 调度**(mandatory 锚点 vs director beat vs 机制 vs ambient);④ **「应用后 mandatory 锚点仍可达」运行时校验**(取代 L3 非干涉);⑤ **长程漂移自动化 benchmark**(spike §5.2,最大未知)。

## Goal
给定一份 immutable backbone(thesis + prohibited outcomes + mandatory 锚点),让 LLM director 据世界状态 + plot ledger **自主生成符合走向、不违禁忌、不饿死 mandatory 锚点的剧情事件**(超越预设),经确定性校验后应用;可复现、可降级、长程可验。**这是总目标③「剧情自动发展」的终点。**

---

## 设计 Q（待御主拍 —— 先列清,不实现）

### Q1 — backbone schema
backbone 怎么声明?候选:scenario.json 加 `backbone` 段(thesis 文本 + `prohibited_outcomes: [...]` + `mandatory_anchors: [event_id...]` + 关键派系/角色白名单 + version/hash)。mandatory 锚点 = v1.8 `anchor: true` 的子集(标 `mandatory: true`)?prohibited outcome 如何形式化到可校验(纯文本给 LLM 看 vs 结构化谓词可机器校验)?**Hassan 倾向**:结构化 mandatory_anchors(机器校验可达)+ 文本 prohibited(LLM 软约束)+ 关键不可逆事实(死亡/归属)结构化禁改集。

### Q2 — director 权限词汇（最关键)
director 提议的事件能 mutate 什么?
- (a) 仅「软」状态(关系/flag/var/flavor NPC)—— 略宽于 L3,但不能真推进主线大事件。
- (b) 「软」+ 受限「硬」(可触发 world_event、引入次要 NPC、局部派系摩擦),但**禁**不可逆 backbone 级(死亡/归属/灭门)+ 禁 player-exclusive。
- (c) 近乎全权,仅 backbone prohibited + mandatory 可达兜底。
- **Hassan 倾向**:**(b)** —— L4 要能「演」(产生方向性事件),但 backbone 级不可逆 + 玩家关键抉择保留。具体词汇 + 「不可逆」判定(哪些 effect 算不可逆)待拍。

### Q3 — 可达性守门（L4 根本难点)
director 改了走向后,如何保证 mandatory 锚点仍可达?
- L3 的「不改任何未来事件 condition」**不适用**(director 就是要改)。
- 候选:**应用前**对每个 pending mandatory 锚点,验「在 director 提议应用后的世界,仍存在一条满足其前置的可达路径」(可能需前向模拟 / 约束求解)。失败 → 拒绝该提议(spike §3.4 snapshot revision/precondition retry)。
- **Hassan 倾向**:M1 先做**保守版** —— mandatory 锚点的前置谓词读集合**禁止被 director 提议的 effect 写**(类似 L3 非干涉,但**只**护 mandatory 锚点,不护普通事件 → 给 director 留演化空间);完整「前向可达模拟」留后续 milestone。**待拍**:保守非干涉 vs 真前向模拟。

### Q4 — plot ledger 设计（长程连贯)
spike §2 分层记忆:backbone / 世界快照 / **plot ledger**(unresolved threads / promises / secrets-who-knows / 完成锚点 / 废分支 / 因果事实,带 ID+source+时间+失效)/ rolling digest(近期逐字 + 远期 arc 摘要,不可逆事实单独保全)/ director plan state(当前 arc / 目标张力 / 下一 beat 意图 / cooldown / 上次接受拒绝理由)。
- 全建 vs 分阶段?**Hassan 倾向**:M2 先 plot ledger 核心(threads + 完成锚点 + 不可逆事实)+ rolling digest;director plan state + secrets-who-knows 后置。每条事实**结构化 + 稳定 ID**,可被检索/失效,**绝不**让生成 prose 回流成事实(继承 L3 反馈环禁令)。

### Q5 — 持久化 / 确定性 replay
director 提议(事件 + effects + ledger delta)固化进 save,reload 确定性 replay 不重询(继承 L3 冻结缓存)。键怎么扩(L3 = gap+月+locale;L4 director beat 不在 gap 内,需 director-turn 序号 + plan 版本 + locale)?spike §5.4:模型不可得 / 版本变更时能否 replay → 存 normalize 后事件 + ledger delta(非模型原始响应/思维链),replay 只读已存。**Hassan 倾向**:director-turn 索引 + backbone hash 入键;reload 走已固化 director 事件序列。

### Q6 — 调度 / 优先级仲裁
spike §3 月序:机制 phase → fire mandatory 锚点 → post-anchor 快照 → 按 cadence/budget/trigger 问 director → 校验(backbone/可达/重复/cooldown/允许 effect)→ 串行应用 accepted → ambient(压制与 mandatory/director 冲突者)→ finalize。优先级类见边界 #4。**Hassan 倾向**:复用 v1.8 phase hook 位置 + cadence;新增 mandatory-anchor-first 排序 + director phase 在其后。

### Q7 — 一致性 critic（spike §2 收尾)
确定性校验(backbone/可达/schema)**之后**,是否再跑一个低成本 LLM **consistency critic**(查矛盾/复活死者/重复 beat)?**Hassan 倾向**:M4 加,确定性校验为主、critic 为辅(不阻塞,失败仍可降级)。

### Q8 — 长程漂移 benchmark（spike §5.2,最大未知)
如何**自动检测**几十年尺度的漂移 / 遗忘死亡 / 重复 beat / 违反锚点?spike §4 只验了 4 轮短程。**Hassan 倾向**:专列一个 milestone 做长程仿真 harness(跑 N 年,断言:所有 mandatory 锚点触发 / 无死者复活 / 无 backbone 违禁 / beat 多样性阈值)。这是 L4「可验」的核心交付,也是最大工程未知。

### Q9 — 成本 / 延迟预算
spike §5.5 + §3:LLM 延迟不得长持模拟锁 → 提前规划数 beat(revisioned snapshot)或短超时跳过。per-turn token/call 预算 + cadence(继承 v1.8)。**Hassan 倾向**:同步有界批量(继承 L3 Q1b)起步;异步 plan-ahead 留后续。

### Q10 — prompt 注入 / mock（继承 v1.8)
继承 v1.8:authored/backbone 字段 `_clip` + fenced 净化(Q10);可注入 director generator + fixtures(成功/超时/坏输出/违 backbone/违可达)不烧真 LLM。

---

## 提议 Milestones（待 Q 拍板后细化,tentative)

- **M0(gate,codex 修正版)** — backbone **结构化** schema(`mandatory_anchors` + `prohibited_predicates` + protected IDs + 不可逆事实集 + version/hash)+ **非空过 proposal boundary**(snapshot-only + 独立 director command schema + 专属 applier + backbone 校验 + 可注入 mock,**带非空过见证**:director 真被调、≥1 proposal 被接受并应用了 scoped fact)+ **deterministic forward replay harness**(同初始态/RNG,director-on/off 重放至 pending mandatory,断言完整 mandatory ID 集 + 触发月 + 禁忌事实一致)+ 三类 negative fixture(indirect blocks / branch condition / mod effect + player-exclusive + 违 prohibited)。**Q2 硬权限(受限硬事件)在 forward replay gate 过之前不开** —— M0 先 **bootstrap**:director 只允许写"不被任何 pending timeline/机制读取"的 scoped director facts(M0 只宣称 proposal boundary + backbone schema 不空过,**不宣称 L4 自主演化成立**)。**闸门:M1 起任何 director 生成代码先过 M0**。
- **M1** — director proposal boundary(snapshot-only + 受限 effect 白名单 Q2 + 专属 applier + backbone 校验 + 可注入 mock)；director 关闭时零行为变化。
- **M2** — plot ledger 核心(threads + 完成锚点 + 不可逆事实,结构化 + 稳定 ID + 失效)+ rolling digest（不可逆事实保全）+ 持久化 + 确定性 replay(Q5 键扩)。
- **M3** — 调度 / 优先级仲裁(mandatory-first + director phase + cadence/预算 + ambient 压制冲突)。
- **M4** — 有界长程 context 组装(backbone + 快照 + ledger 检索 + plan state)+ 一致性 critic(option)。
- **M5** — **长程漂移 benchmark harness**(跑 N 年,断言锚点全达/无复活/无违禁/beat 多样)—— L4「可验」核心。
- **M6(option)** — 异步 plan-ahead(revisioned snapshot + staleness)。

## 实现进度

### M0（bootstrap gate）✅ — proposal boundary + forward-replay harness（scoped-facts)
- **mandatory schema**:事件 `mandatory: true`(loader `_validate_anchor`:bool + 必须 `anchor: true`)。测试 `test_scenario_anchor_schema.py`。
- **`src/scenario/narrative_director.py`**:snapshot-only `make_director(call_llm_json, require_key)`(继承 v1.8:严格超时 + 无-key gate + 降级)+ `validate_director_proposal`(**bootstrap 白名单 = `{director_fact}`**,纯叙事/scoped fact;set_flag/world_event_trigger/npc_die 等**全拒**)+ 专属应用(**零机制 mutation**:director_fact 只记 ledger,scoped fact 无任何机制读回)+ 引擎唯一 Event id `director:{month}:{turn}:{idx}`、render-only narration(is_story、content="")。
- **phase hook**:`apply_narrative_director` 在 dispatch + narrative_fill + transition **之后**(mandatory 锚点已优先触发)。无 `world.director_generator` 时 no-op。
- **审计 ledger**:`ScriptedScenarioState.director_ledger`(state 兄弟,持久 save/load;plot-ledger 正式化留 M2)。
- **forward-replay gate `test_scenario_director_gate.py`**:director-on vs off,**mandatory 锚点完整 ID 集 + 触发月一致**;**非空过见证**(director 真被调 + 接受 proposal + 记 scoped fact + 发 director Event);bounded-hard 命令被拒(记 reason、零 Event、零机制)。
- **范围诚实**:M0 **只宣称** proposal boundary + backbone schema 非空过 + harness 成立,**不宣称 L4 自主演化成立** —— bootstrap director 无机制权限,故 mandatory 可达**平凡**成立但**非空过**。Q2 硬权限 + Q3 真前向重放可达守门 = M1。
- full suite **1941 pass / 2 skip**,零回归。codex 二审在 M1 dispatch(同节奏)。

### M0 codex 二审（FIX-FIRST,均为测试缺口 → 已补,2026-06-16)
codex **确认实现正确**(零机制 mutation ✅ / snapshot-only·无-key·降级 ✅ / mandatory schema ✅ / phase 顺序 ✅),两处**测试**加固:
- **forward-replay gate 的 M1 空过风险(已补)**:原 gate 的 mandatory 全 `always`,证不了"M1 开硬权限后 harness 会变红"。补**非空过哨兵**:mandatory `m2` 的 condition 读 `world_flag blocker`,director 提议一个(被拒的)`set_flag blocker`。M0 下越权命令被拒 → m2 仍触发;**M1 一旦错误放开该命令且无可达守门,同一 gate 即变红**(m2 被饿死)。这让 harness 真正夹住一个对 director-writable state 敏感的 mandatory 锚点。
- **director 专属 chronicle 回归(已补)**:断言 director Event 带 `is_story=True` + 空 content(exclude_story 查询依赖的属性)→ director burst 不挤占 authored 编年史(复用 v1.8 exclude_story 硬保证)。
- full suite **1943 pass / 2 skip**。

> **M0 闸门成立(经 codex 二审)**:M1 起开 Q2 受限硬权限时,forward-replay gate(含哨兵)升级为「director 改走向后 mandatory 仍可达」的真守门(非平凡),harness 已就位。

### M1a（第一个硬命令 + 前向重放可达守门)✅ — 御主拍板「方案 A 有界条件态 dry-run + fail-closed」
- **第一个受限硬命令 `director_set_flag`**(`DIRECTOR_HARD_COMMAND_WHITELIST`):写真 world flag(真机制效果,能改走向)。区别于 M0 的 scoped fact。
- **`mandatory_reachable_after`(Q3 守门,有界条件态 dry-run)**:复制条件态(world_flags/scenario vars/relations/triggered/storylines),从当前月**前向重放 scenario dispatch** 到最后一个 pending mandatory 锚点月,**with vs without** director 的 flag,要求 mandatory 触发(ID 集 + 月)**逐一一致**。世界/NPC/玩家态**不复制** → 改用 **fail-closed**:horizon 内任一 not-fired 事件若**不可完全建模**(非 modellable predicate / 非 modellable effect / 含 choices)→ 拒该命令。固定 seed → random_chance 两轮一致,只有命令是变量。
- **集成**:`apply_narrative_director` 对 `director_set_flag` 先过 gate,通过才写真 flag,否则记 reject reason。
- **测试**:gate **拒**会饿死 mandatory 的 flag(m2 读该 flag → 仍触发、flag 未设)、gate **放行**无 mandatory 读的安全 flag(真应用)、**fail-closed** 于非 modellable horizon(player_stat 事件 → 拒)。full suite **1945 pass / 2 skip**。
- **范围**:M1a 只开 `director_set_flag` 一个硬命令 + 守门 harness。relation/introduce_minor_npc/emit_director_event/local_crisis = M1b;结构化 prohibited_predicates + 不可逆事实集(Q1/Q4)= M1b/M2;full-world 保真 dry-run(方案 B)出 scope(方案 A fail-closed 是诚实有界版)。

> **L4 真守门成立**:director 现能写真 flag 改走向,但**只在前向重放证明所有 mandatory 锚点仍可达时**;不可建模的 horizon 一律 fail-closed。M0 哨兵在此真正发挥作用(从 whitelist-reject 升级为 gate-reject)。

## 验收（草案）
- 给定 backbone,director 在 mandatory 锚点之间/外**自主生成**符合走向、不违 prohibited、当前剧本语境的方向性事件。
- **所有 mandatory 锚点按时触发**,无论 director 是否生成(差分守门,跨多 tick)。
- director 提议违 backbone / 会饿死 mandatory 锚点 → **被拒**(negative test)。
- 长程仿真(N 年)无死者复活 / 无 backbone 违禁 / beat 多样性达阈(M5 benchmark)。
- reload 后 director 事件序列 + effects 不变,不重询 LLM(确定性 replay)。
- no-LLM / 无 key → 退化为 mandatory 锚点纯骨架,正常可玩。
- full pytest 零回归;跨 schema backbone 原语可 load。

---

---

## codex 二审纪要（2026-06-16,第一轮 —— vet 决策表 + 三类风险)

**总判定:方向成立,Q3/M0 必改才可锁。** Q2 CONCUR-with-constraints;**Q3 DISSENT(必改,已采纳)**;Q4 CONCUR-with-escalation;余 CONCUR + 加固。已全折入上表。

- **Q3 DISSENT(最关键,已改)**:保守"只护 mandatory 锚点前置读集合"在 v1.9 放开 Q2 权限后**不够**,且**正好重开 v1.8 整体二审已修的三桥**:① director 写 flag/var/relation → 普通未来事件 B 触发 → B `blocks_events` mandatory;② director 改 branch/choice 条件输入 → 换分支 → 分支 effect 激活 storyline/set_flag/trigger → mandatory 不可达;③ 普通 future event 带注册 mod effect 读 director 写的状态再写 backbone 相关态。v1.8 最终靠"护所有 future 事件 trigger/branch/choice 读集合 + mod effect fail-closed"闭合;v1.9 明确解除非干涉 → **必须上真前向重放 gate,或诚实降低 M0 权限**。→ 已改 Q3 = deterministic forward replay;M0 = bootstrap(硬权限延后)。
- **Q2 修正**:不复用 `world_event_trigger`(写 `triggered_event_ids` 能伪造 mandatory 已触发,`effect_applier.py:165`)→ 新建 director command schema。
- **Q1 修正**:`prohibited` 文本不能当硬 gate → 加结构化 prohibited predicates + protected IDs + 不可逆事实集。
- **Q4 升级**:不可逆事实集必须在任何受限硬事件**启用前**就位(否则复活/归属回滚/摘要复活无法 deterministic gate),不能拖到 M2 后。
- **Q5 加固**:reload 须存 backbone snapshot/hash;hash 不符 → 冻结旧 backbone 重放 或 拒绝/降级,绝不用新 backbone 解释旧 ledger。
- **Q6/Q8 加固**:Q6 须新建真实中心调度仲裁(现 dispatcher 单趟扫不够);Q8 长程 benchmark = **merge gate**,非可选。
- **M0 可建性**:原 M0(同时承诺"广义可达"却只做保守读集合)会**空过/假绿**。codex 给两条可建路径,已采纳「**M0 = backbone schema + 非空过 proposal boundary + forward replay harness;Q2 硬权限延后到 gate 过**」。

*下一步:dispatch 已完成 → 决策表 + M0 已按 codex 收敛。御主确认收敛版 → 起 M0(codex 修正版)。本版仍仅 spec,未实现。codex resume: `019ecde0-92e7-7ce0-bb0b-e5e019d63243`。*
