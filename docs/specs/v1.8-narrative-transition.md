# v1.8 Spec — L3 剧情衔接 LLM 生成（Narrative Transition / Anchor Interpolation）

## Status
Draft（收敛版,御主拍板「按推荐走」2026-06-14)— Hassan。Prereq: v1.7（L2 LLM Narrative Fill）✅ on main（@47c8f41a）。
**codex 二审完成(8 CONCUR / 3 DISSENT,见文末纪要),裁决全折入。御主拍板按推荐 → Q2 白名单/Q5 ledger 键/M0 闸门范围按下表收敛值锁定。实现起点 = M0。**

> 起草依据已落地基建:v1.6 branch/storyline + load 时结构校验、v1.7 render-only narration 通道 + 可复现缓存 + 净化沙箱 + 可注入 generator;以及 **v1.9 narrative-director spike**(`docs/specs/spike-narrative-director-report.md`,架构 §1-3 + minimax M2.7 4 轮滚动实证)。v1.8 是该 spike 的**受约束子集**:只在锚点之间插值,不越锚点。

---

## 边界（锁死,先于一切）

> **v1.8 = LLM 生成「锚点之间」的过渡剧情。剧本定大走向(锚点),LLM 填中间。半 AI。**

这是阶梯上第一次让 LLM 碰**「发生什么」**——但**被锚点夹住**。和 v1.7 的根本区别:

| | v1.7 (L2) | v1.8 (L3) |
|---|---|---|
| 走向(发生什么) | 100% scripted | **锚点 scripted;锚点之间 LLM** |
| LLM 输出 | 仅 narration 展示文本 | 过渡 **beat(受限机制 + narration)** |
| 机制不变量 | fill 开/关 effects 逐字节一致 | **锚点必触发 + 写集合不干涉锚点前置**(见下) |

**L3 的核心承诺(= 本版全部意义)**:无论 LLM 在锚点间生成什么,**每个锚点都必然在其计划月触发,且触发时其前置成立**。LLM 可丰富中段,但**绝不能改变剧本大走向**,也**绝不能让任何后续锚点变得不可达**。

**硬不变量(架构级,不可越界)**:
1. **锚点至上**:锚点是确定性骨架(v1.6 storyline/scripted 事件)。过渡 beat 优先级**永远低于锚点**;同月冲突时 beat 让路,锚点不动。**注意**:dispatch 现按 timeline 顺序、无优先级类(`event_dispatcher.py:49`)——`hard_anchor` 优先级**未落地**,必须新建(见 M0/M1),否则「同月 beat 排在锚点前 → 篡改其 condition/triggered」(starvation #9)。
2. **写集合非干涉(codex 修正 Q3,关键)**:过渡 beat 的**写集合(它会改的 flag/var/relation/entity/triggered/storyline)不得与任何 pending 锚点的前置谓词读集合相交**。守门**不是**「应用后测下一锚点 condition 是否为真」——锚点 condition 到期前本就该为假,且 dispatch 是一次性精确月触发(`event_dispatcher.py:71-108`),当前真值测试既会误杀也会漏检。守门 = **静态 write-set/非干涉校验 + 定时重放**(beat-on vs beat-off,完整锚点 ID 集在同 (year,month) 触发)。
3. **transition 命令 schema,非通用 effect lane(codex 修正 Q2,关键)**:beat 的机制**不走** `apply_effects()`。原因:`apply_effects()` 对非可 pickle 活对象**跳过 rollback 快照**(`effect_applier.py:214-237`)→「先应用再校验再回滚」不安全;registered mod effects 是任意 callable,按 effect 名挡不住(`effect_applier.py:_apply_one`)。改为**独立的、白名单受限的 transition 命令 schema**,经 schema/ID/write-set/staleness 校验后,由专属 applier **确定性应用**。
4. **不可变快照 proposal 边界(codex 修正)**:generator **不接收可变 `world` 句柄**(现状 `filler(requests, world)` 收活 world,`scripted_scenario.py:177-183`,与本边界冲突 → 必须新建 snapshot-only 接口)。generator 收**不可变序列化快照**,只返回**数据**(narration 文本 + 涉及实体 ID + transition 命令 + 优先级/过期),不写代码、不直接 mutate world。
5. **文本隔离**:beat 的 prose **只进 `Event.narration`**(v1.7 已隔离的 render-only 通道),**绝不进 `Event.content`**(content 喂编年史 + AI context,`event.py:36-43` / `narrative_fill.py:_chronicle_context`,回流即污染走向)。beat 文本/计划/缓存元数据**绝不进 `ScriptedScenarioState.state`**(被 `var_equals` 读到,`state_access.py:86-107`)。
6. **降级即纯剧本**:无 LLM key / 失败 / 超时 → 跳过过渡生成,锚点照常按时触发,游戏完整可玩(过渡是连接组织,非必需;不需 authored fallback 文本)。

> v1.9 (L4) 才让 LLM **超越**预设锚点自主演化走向。v1.8 严格是「**锚点间插值**」:LLM 知道从锚点 A 走向锚点 B,只填中间,不改 A/B,不改 A→B 大方向。

---

## 决策（codex 二审后收敛 —— 御主拍板「按推荐走」2026-06-14,下表锁定)

| # | 决策 | 收敛值 |
|---|---|---|
| **架构** | beat 机制如何应用 | **独立 transition 命令 schema + 专属确定性 applier**,不复用 `apply_effects()`(rollback 不安全 + mod effect 不可控) |
| **架构** | generator 接口 | **snapshot-only**:收不可变序列化快照,返回结构化 proposal 数据;**新建**接口,不直接复用 v1.7 `filler(world)` |
| Q1 | 锚点表达 | **显式 `anchor: true`**(+ 可选 `protected_preconditions`/`protected_outcomes`);引擎识别相邻锚点间的可插值 gap。**新增 schema,未落地** |
| Q2 | beat 机制权限 | **(b) 受限白名单**,实现为 transition 命令 schema。初始白名单**排除** `world_event_trigger`/`activate_storyline`/flags/vars/死亡·境界·势力变更/registered mod effects/**任何写集合与 pending 锚点谓词相交的操作**。relationship delta 仅当该 relation pair 不在任何 pending 锚点依赖中、且 delta 有界时允许 |
| Q3 | 可达性保证 | **静态 write-set/非干涉校验 + 定时重放守门**(非当前真值测试);新建 write-set 依赖校验器(v1.6 校验纯结构性,不够) |
| Q4 | 生成时机 | **cadence + 有界批量**,generator 严格 ≪超时 + per-tick 预算;**+ staleness/revision 校验**(v1.7 无 snapshot revision,批量计划会过期) |
| Q5 | 持久化/replay | **独立 normalized beat ledger**(effects + 涉及实体 + 顺序 + 接受状态),**非** `Event`(无 effects 字段,`event.py:45-62`)、**非** `state`;reload 走 ledger 确定性 replay,**不重询 LLM** |
| Q6 | 连贯记忆 | L3 轻量:backbone + snapshot + **下一锚点目标(预留空间、不被截断)** + 本 gap 已生成 beat 的 normalized 事实(非 prose)。复用 v1.7 M3 有界组装,但需保留高优先层。完整 plot ledger 出 scope(L4) |
| Q7 | 失败降级 | **跳过该 gap 过渡,锚点照常触发**;无 authored fallback。前提:生成**不得延迟或标记锚点** |
| Q8 | 守门/验收 | 锚点必触发 + effect ∈ 白名单 + 写集合不干涉 + 确定性 replay。**测试须带:非空过见证(断言 generator 被调、≥1 beat 被接受并应用非零白名单 effect)+ 确定性初始状态/RNG + 比对完整锚点 ID 集** |
| Q9 | prompt 注入 | 复用 v1.7 `_clip` + fenced 数据区(`narrative_fill.py:31-35,118-152`);transition schema/snapshot 标签/锚点目标/前 beat 事实**全部**同等净化 |
| Q10 | mock generator | 复用可注入 callable,但返回类型从 `{event_id: text}` 扩为**结构化 beat proposal**;fixtures:成功/超时/坏输出/**违规 beat(试图饿死锚点)→ 被拒**/未命中 |
| Q11 | locale | 复用 v1.7 缓存键含 locale(`narration_cache.py:27-36`);**规划 gap 时显式冻结 run 的 content_locale**(现读全局 `language_manager.current`,`scripted_scenario.py:155-164`) |

---

## Context / 阶梯定位

- roadmap **L3**(`roadmap-to-narrative-goal.md` line 41):剧本只写关键**锚点**事件,LLM 生成锚点之间的过渡剧情;剧本定大走向,LLM 填中间;半 AI。
- **咬合下层(codex 校准过的真实咬合点)**:
  - **v1.6 branch/storyline**:给了骨架结构 + 整线互斥。但**无** anchor-chain 查询 / next-anchor 解析 / 优先级排序 / retry 语义 / gap 抽象 —— 全需新建。
  - **v1.6 可达校验**:`_validate_storyline_reachability`/`_validate_storyline_structure`(`scenario_loader.py:927-1027`)**纯结构性**(activator 存在 / 环 / 兄弟数),**不**验时间/条件/依赖/effect 干涉 → v1.8 需**新建 write-set 依赖校验器**(我原 spec「扩展即可」过于乐观,已改)。
  - **v1.7 M1 generator + 通道 + 沙箱**:narration 通道复用正确;但 generator 现收**可变 world**、返回**字符串** → 需 snapshot-only + 结构化 proposal 新接口。沙箱 `_clip`/fenced 复用正确。
  - **v1.7 M2 缓存**:仅适用于 prose(键到 authored event/outcome 的字符串),**无法**持久化 effects/过期/实体/接受状态/beat 顺序 → 需独立 ledger。
  - **v1.7 M3 有界 context**:方向对,但 `_assemble_within_budget()` 会整块切片(`narrative_fill.py:99-115`)→ 「下一锚点目标」须预留空间 + 高优先,否则被截断(starvation #10);per-tick **token** 预算**未落地**(现仅字符级),beat 预算需新建,且要 anchor-reserved 配额防 budget starvation(#11)。
- **复用 spike**:`tools/spike_narrative_director.py` 已实证 minimax M2.7 能连贯顺剧本生成结构化 JSON 剧情(4 轮滚动跨轮记忆成立)。spike §1-3 proposal boundary / 记忆分层 / Director-Simulator 协调 = v1.8 蓝图;v1.8 取其受锚点夹持子集。

## Goal
让作者只标关键**锚点**(走向骨架),引擎在相邻锚点之间按 cadence 调用 LLM 生成**过渡 beat**;beat 经 schema/write-set/staleness 校验后由专属 applier 确定性应用,文本走 v1.7 narration 通道;生成可复现、可降级。**承诺:锚点必触发、写集合不干涉锚点前置、无 LLM 时纯剧本可玩。**

---

## 设计 Q（全文保留作决策依据 + codex 裁决标注）

### Q1 — 「锚点」如何表达 — codex CONCUR
显式 `anchor: true`(默认 false) + 可选 `protected_preconditions`/`protected_outcomes`;引擎把相邻两锚点间的月识别为可插值 gap。锚点可落在 storyline 上(选定线的锚点链)。**codex**:现 dispatch 按 timeline 顺序、无 anchor/优先级概念(`event_dispatcher.py:49`),显式字段优于重载普通事件;但 `anchor`/`protected_*` 均未落地。

### Q2 — 过渡 beat 的机制权限 — codex CONCUR（修正为命令 schema）
(b) 受限白名单。**codex 关键修正**:不是把 effect 子集塞进 `apply_effects()`,而是**独立 transition 命令 schema**。canonical effect lane 含改向操作 `world_event_trigger`/`set_var`/`activate_storyline`/`npc_die`(`effect_applier.py:_apply_one`;`schema_constants.py:25`),且 `apply_effects()` 对活对象跳 rollback 快照(`effect_applier.py:214-237`)、mod effect 是任意 callable → 通用 lane 不安全。初始白名单见决策表 Q2。

### Q3 — 锚点可达性如何保证 — codex DISSENT（重写）
**原方案(已废)**:应用前测下一锚点 condition 是否为真。**codex 反驳(采纳)**:锚点 condition 到期前合法地为假;dispatch 一次性精确月触发(`event_dispatcher.py:71-108`),当前真值测试不能证明未来可达。**新方案**:① 静态 **write-set/非干涉**校验(beat 写集合 ∩ 任一 pending 锚点谓词读集合 = ∅);② **定时重放**守门(beat-on/off,完整锚点 ID 集同月触发,见 Q8/M0)。配合 (b) 白名单双保险。

### Q4 — 生成时机 / cadence / 预算 — codex CONCUR（补 staleness）
cadence + 有界批量,generator 严格 ≪超时(scenario phase await 内联,v1.7 可阻塞 ~30s,`simulator.py:92-94`/`scripted_scenario.py:124,181-186`)+ per-tick 预算。**codex 补**:批量从初始快照生成,earlier beats / 普通 sim phase 会使其失效 → **需 revision/staleness 校验**(v1.7 无 snapshot revision)。逐月同步否决。

### Q5 — 可复现性 / 持久化 — codex DISSENT（重写）
**原方案(已废)**:beat 当普通 fired event 固化即可 replay。**codex 反驳(采纳)**:`Event`/`EventStorage` **无 effects 字段**(`event.py:45-62`/`event_storage.py:82-92`),load 恢复世界状态而非 replay 事件(`load_game.py:125-150`)→ fired event + triggered ids **不能**重放 beat 机制。**新方案**:独立 **normalized beat ledger**(effects + 涉及实体 + 顺序 + 接受状态 + 过期),**非** state(防 `var_equals` 读)、**非** Event;持久化 normalize 后数据(非模型原始响应/思维链);reload 走 ledger 确定性 replay,不重询 LLM。键:`(scenario_id, gap=(prev_anchor_id,next_anchor_id), beat_index, resolved_path, content_locale)`。

### Q6 — 连贯 / 记忆 — codex CONCUR（补预留 + 事实非 prose）
L3 horizon 有界(当前锚点→下一锚点),轻量四件:backbone + 世界快照(ID+短标签) + **下一锚点目标** + 本 gap 已生成 beat。**codex**:`_assemble_within_budget` 会整块切片 → 「下一锚点目标」须**预留空间 + 高优先**,否则被截断;accepted-beat 记忆须存 **normalized 事实,非生成 prose**(防 prose 回流成走向输入)。复用 v1.7 有界组装 + chronicle 仅 authored。完整 plot ledger 出 scope(L4)。

### Q7 — 失败 / 降级 — codex CONCUR
跳过该 gap 过渡,锚点照常触发(不变量 #6)。无 authored fallback(过渡是 opt-in 增量)。**codex 前提**:生成路径**不得延迟或标记锚点**(现 narration 失败已优雅降级,`scripted_scenario.py:173-206`,可借鉴)。

### Q8 — 守门 / 验收 — codex DISSENT（强化非空过)
不变量(锚点必触发 / effect ∈ 白名单 / 写集合不干涉 / 确定性 replay)方向对,但**测试规格不足**。**codex 强制**:① **非空过见证** —— 断言 generator 被调用、≥1 beat 被接受、应用了**非零**白名单 effect;② **确定性初始状态 + RNG**(对抗 `random_chance` 谓词);③ 比对**完整锚点 ID 集**(非只比触发了的,exact-month 静默丢失才能抓到);④ 同测试内放**一个已知允许 + 一个已知禁止**的 effect(防白名单断言对空序列恒真);⑤ 锚点前置须用真谓词(`world_flag`/`var_equals`/relation/NPC/`event_triggered`),**禁用 `always`**(现 storyline fixture 多用 `{"always":{}}`,证不了前置保持)。

### Q9 — Prompt 注入 — codex CONCUR
复用 v1.7 `_clip` + fenced 数据区。transition schema / snapshot 标签 / 锚点目标 / 前 beat 事实**全部**同等净化。

### Q10 — Mock / fixture — codex CONCUR（扩返回类型）
可注入 generator(`make_narrative_filler(call_llm=...)` 已证,`narrative_fill.py:155-180`);但返回类型 `{event_id: text}` **不能直接复用**于结构化 beat → 扩为 beat proposal。fixtures:成功/超时/坏输出/**违规 beat 被拒(断言 call count + proposal 收到 + 拒因 + 零 effect 应用)**/未命中。

### Q11 — Locale — codex CONCUR
按 `content_locale` 生成,缓存键含 locale(`narration_cache.py:27-36`)。**codex**:production 现读全局 `language_manager.current`(`scripted_scenario.py:155-164`)→ 规划 gap 时**显式冻结** run 的 content_locale。

---

## 提议 Milestones（收敛后）

- **M0(gate)** — **锚点概念 + `hard_anchor` 优先级排序 + 跨锚点定时重放差分测试,同一 milestone 一起落**。差分:beat-on vs beat-off,**完整锚点 ID 集**同 (year,month) 触发、触发时前置一致。**带 codex 强制的非空过见证**(generator 被调 + ≥1 beat 接受 + 应用非零白名单 effect + 确定性 RNG + 真谓词锚点前置)。**闸门:M1 起任何生成代码先过 M0**;且优先级排序/anchor 识别须先落,差分测试才不空过。
- **M1** — `anchor` schema(显式 bool + protected 前置/产出)+ **write-set 依赖校验器**(load 时,扩展 v1.6 结构校验)+ **transition 命令 schema + 专属确定性 applier**(不走 `apply_effects`)+ **snapshot-only generator 接口**(收不可变快照、返回结构化 proposal)+ 可注入 mock;**生成关闭时零行为变化**。
- **M2** — write-set 非干涉**运行时**校验(违规 beat 拒绝)+ 有界批量/cadence 生成(非逐月,严格超时 + per-tick token 预算 + anchor-reserved 配额)+ **staleness/revision 校验** + 降级路径(无 key→跳过,锚点照常)。
- **M3** — 独立 normalized beat ledger(effects + 实体 + 顺序 + 接受状态,非 state/非 Event)+ 确定性 replay(reload 不重询 LLM)+ prose 走 v1.7 narration 缓存(键含 gap/locale,冻结 run locale)。
- **M4** — bounded 连贯 context(backbone + snapshot + **预留高优先**下一锚点目标 + 本 gap normalized beat 事实;chronicle 仅 authored,无反馈环)。
- **M5(option)** — 一致性 critic(spike §2);多 beat 提前规划(revisioned snapshot)。**完整 plot ledger 出 v1.8 scope(L4)。**

## 实现进度

### M0（gate）✅ — anchor 概念 + write-set 非干涉 + 非空过差分
- **anchor schema**：事件显式 `anchor: true`（loader `_validate_anchor`，bool 校验；测试 `test_scenario_anchor_schema.py`）。
- **transition 命令 schema + 专属确定性 applier**（`src/scenario/narrative_transition.py`，**不走** `apply_effects`）：`TRANSITION_COMMAND_WHITELIST = {relation_delta}`（Q2 锁定初始集，排除 flags/vars/storyline/world_event_trigger/死亡·境界·势力/mod effects）；`relation_delta` 有界（`RELATION_DELTA_MAX=10`）。
- **write-set 非干涉（M0 核心不变量）**：`condition_read_set`（提取锚点前置读的 relation/flag/var/event）+ `command_write_set` + `pending_anchor_read_set`；`validate_beat` 拒绝「命令不在白名单 / 超界 / 写集合与任一 pending 锚点前置相交」。
- **snapshot-only generator 接口**：`world.transition_generator(snapshot)` 收不可变快照（非活 world）、返回结构化 beat proposal；有界 budget + 严格超时（30s）+ 失败/超时降级跳过（锚点已先触发）。prose 仅入 `Event.narration`（`is_story=True`、`content=""`，不进 AI 记忆/编年史）。
- **gap 检测**：anchors 已 dispatch 后才生成（优先级）；仅在「已过 ≥1 锚点 + 有 pending 锚点 + 本月无锚点触发」的 gap tick 生成。
- **审计 ledger**：`ScriptedScenarioState.transition_ledger`（state **兄弟**字段,非 state 内；记 accept/reject + reason + applied command）。持久化留 M3。
- **差分 gate（非空过）`test_scenario_narrative_transition_gate.py`**：beat-on vs off 完整锚点 ID 集同月触发；**非空过见证**（generator 被调 + ≥1 beat 接受 + relation 实际 +5）；白名单违规 beat 被拒（flag 未设）；写集合违规 beat 被拒（锚点保护的 hero↔rival 关系未动 → A2 仍触发);真谓词锚点前置(`npc_relation`,非 `always`)。
- full suite **1891 pass / 2 skip**,零回归。codex 二审在 M1 dispatch（同 v1.7 节奏）。

### M0 codex 二审（FIX-FIRST → 已修,2026-06-14）
codex 静态审查 M0 实现,判 FIX-FIRST,挖出守门机制自身的真漏洞(gate 给假保证)。已修:
- **#1 player_relation 别名错配(核心 bug)**:read-set 用 `@player` 哨兵但求值用真玩家 id → beat 对 `(player_id, npc)` 可绕过保护饿死锚点。修:`pending_anchor_read_set` 在生成时用 `get_player(state)` 解析真 id 替换哨兵。
- **#2/#3 read-set 漏提取 → 改 fail-closed**:`var_equals` 读整个 `sc.state`(含 relations);mod 谓词读任意状态而 `condition_read_set` 返回空。修:引入 `PROTECT_ALL` 哨兵 —— 未知/mod 谓词、`var_equals` 读结构性子对象(relations/flags/storylines)、畸形表达式一律 fail-closed,拒绝一切有状态 beat。仅对「确定 relation-无关」的已知谓词返回空集。
- **#4 过期锚点 runaway**:`_is_gap_tick` 不看锚点年月 → missed 锚点令 gap 永久开、generator 无限跑。修:gap 与 protected 仅计 future-or-current 锚点(`trigger >= now`),past-due 视为已错过。
- **#6 validate_beat 加固**:relation_delta 要求 a/b 非空且相异、delta 为非零 int(拒 bool/float)、有界;`PROTECT_ALL` 命中即拒。
- **#8 chronicle 间接污染**:beat Event 经 finalizer 入 EventManager,占 `get_recent_events` 槽位挤掉 authored 事件。修:`narrative_fill._chronicle_context` 过滤 `is_story`(beat=display flavor,绝不进 authored 编年史窗口)。
- **测试加固(对照 codex 7 条空过清单)**:ledger accept/reject + reason 断言、forbidden beat 不产 Event、generator 仅 gap tick 被调(spy 验月份 = [2,3])、fail-closed 单测、player 解析单测、validate_beat 畸形拒绝单测。full suite **1898 pass / 2 skip**。
- **诚实下沉(非 M0 blocker,不overclaim)**:**#5 同月 authored 锚点排序** = v1.6 既有 backlog(P1-c),与 beat 无关(beat 在 dispatch 后跑,已天然低于锚点);spec 不再声称 M0 交付「hard_anchor 重排」,intra-timeline 排序留 v1.6.x/M1。**#7 snapshot 闭包捕获 world + 同步 generator 不受 wait_for 超时** = 接口固有(无法阻止恶意闭包;生产 generator 为 async LLM),按约定 + 注释处理,真线程级超时留后续。**ledger 持久化 = M3**。

> **M0 闸门成立**(经 codex 二审 + 修复):M1 起任何生成代码先过本 gate。

### M1（load 时 timed 锚点可达校验）✅
M0 已落 command schema / 专属 applier / snapshot-only 接口 / mock,M1 真正剩下的硬交付 = **load 时的锚点链 timed 可达校验器**(把 codex starvation #1 exact-month 丢失、#5/#9 同月反序依赖从运行时 backlog 升级为 load hard error)。
- **`_validate_anchor_reachability(events)`**(`scenario_loader.py`,在引用完整性 pass 之后、storyline 校验之前):对每个 anchor,其 `requires_events` 每一项必须排程在该 anchor 的 exact-month 窗口**之前或同月更早 author order**,否则单趟一次性 dispatch 永远满足不了 → load 报错。两种失败:required 在更晚 (year,month);required 同月但 author order 在 anchor 之后。
- **范围**:仅守 anchor 骨架(非 anchor 事件保持 v1.6 既有较松行为,不在 M1 回归目标内);纯 timing,**补充**而非替代 M0 运行时 write-set 非干涉(后者守生成 beat 不破坏锚点**条件**可达)。
- 测试 `test_scenario_anchor_reachability.py`(6:earlier-ok / later-reject / same-month-reverse-reject / same-month-correct-ok / 非 anchor 不受限 / anchor-requires-anchor)。full suite **1904 pass / 2 skip** 零回归。无 shipped scenario 用 anchor → 对现有内容 no-op。

### M1 codex 二审（FIX-FIRST → 已修,2026-06-14）
codex 挖出**比 M1 更深的真问题**:M0/M1 边界未闭合。已修:
- **#1 M0 闭包保护(安全关键)**:M0 原只保护锚点**自身** condition 的读集合;但锚点可达性还依赖其**传递前置闭包**(transitive `requires_events` + storyline activator 的条件)。beat 改了某被依赖事件 R 的 condition 读的 relation → R 失败 → 锚点 requires 不满足 → 饿死,**即使锚点自身是 `always`**。修:`pending_anchor_read_set` 改为对每个 pending 锚点遍历 `_precondition_closure`(自身 + 传递 requires + storyline activator),保护并集。新增 `_activate_storyline_targets` / `_precondition_closure`。
- **#2 M1 传递 + 环(load)**:`_validate_anchor_reachability` 从「仅直接依赖」改为**DFS 锚点 requires 传递闭包**,每条边 X→Y 验 timing,并**检测依赖环**(GREY/BLACK 着色)。非 anchor 一旦进入锚点闭包即收紧。
- **诚实改口(兑现 codex 要求,不再 overclaim)**:M1 **只证明依赖闭包的 timing + 无环**,**不**证明完整动态可达。**下沉为 authored-baseline 校验 backlog**(部分不可判定/易误报):#3 `blocks_events` 静态饿死、#4 timed storyline-activator 可达(v1.6 校验纯结构)、#5 恒假 condition(`always:false`/`not always`/`random_chance:0`)。**L3 的安全契约**(beat 绝不让锚点可达性比 no-beat baseline 更差)由 M0 闭包写集合非干涉兑现,**与 baseline 是否自身无 bug 无关**。
- 测试 +5:传递 timing、requires 环、非 anchor 进闭包被查(loader);closure 保护 required 事件条件 + storyline activator 条件(gate)。full suite **1909 pass / 2 skip**。

### M2（cadence 节奏控制）✅
M0 已落运行时 write-set 校验 + 严格超时 + degrade + beat-count 预算。M2 真正剩下的可实现(mock-driven,无需真 LLM)交付 = **cadence(非逐月)**:
- **`world.transition_cadence_months`**(默认 1 = 每个 gap 月)+ `ScriptedScenarioState.transition_last_gen_month`(总月戳 year*12+month,state 兄弟字段):gap tick 内,距上次调用 generator < cadence 个月则跳过,不询问 generator。长 gap 不再逐月刷 beat。
- 测试:8 月时间线(锚点 M1/M8,gap M2-M7),cadence=3 → generator 仅在 M2、M5 被调(`seen==[2,5]`)。默认 cadence=1 保持 M0 行为(既有 gate 测试 `seen==[2,3]` 不变)。
- **预算隔离澄清(codex P2 修正措辞)**:fill `NARRATIVE_FILL_BUDGET` 与 transition `TRANSITION_BUDGET` 是**独立的计数预算**,且同 tick 内 fill 先执行 → transition 不抢本月 fill 的 beat 名额。但二者**共享** LLM client 的全局 semaphore / 配置 / provider 配额(`client.py`)→ 只能说「**计数**预算独立」,**不**是资源/速率隔离。真正的 token/速率预算下沉 M4。
- **P1 修复(codex FIX-FIRST,reload 重置 cadence)**:`transition_last_gen_month` 现进 save/load(`save_game`/`load_game`),否则 reload 重置节流游标 → 同月重新询问 generator → 可能**重复应用 beat 效果**。该字段持久化不能推迟到 M3。
- **下沉 M4(与真 LLM/批量提前耦合)**:per-tick **token** 预算(现仅 beat-count + char)、**staleness/revision**(M0 同 tick 即生成即应用,无批量提前 → staleness 暂不适用;真 LLM 批量提前规划时才需 revision 比对)。
- 测试:cadence 节流、跨年月差、失败仍推进游标(不狂刷)、cadence 游标 save/load 往返。full suite **1913 pass / 2 skip**。

### M3（持久化 + 确定性 replay）✅
- **`transition_ledger` 进 save/load**(`save_game`/`load_game`,顶层 scenario 字段,**非** state,延续 narration_cache 模式)。relation 效果(在 `sc.state["relations"]`)与 cadence 游标(M2)已持久 → M3 补齐 ledger。
- **确定性 replay 模型**:beat 一次性生成即应用,效果落 state(持久)、审计落 ledger(持久)、cadence 游标(持久)。reload 恢复 beat 后世界,继续模拟既不重询 generator 也不重复应用。

### M3 codex 二审（FIX-FIRST,含 P0 → 已修,2026-06-15）
codex 抓出 M3 偏离 spec Q5、且我 overclaim 了"确定性 replay"。已修:
- **P0 — 真冻结缓存(兑现 Q5)**:原只靠 cadence 游标 + 效果持久,**没实现 Q5 锁定的「按 gap 冻结 beat、reload 不重询」**。新增 `ScriptedScenarioState.transition_cache`(state 兄弟,持久),键 `_gap_key = (prev_anchor>next_anchor | YxMx | locale)`。gap 月首次生成即**冻结**;cache **HIT → 重放冻结 beat 做显示,不重询 LLM、不重复应用效果**(效果已在持久 state)。即便空结果也冻结(防 re-entry 重生成发散)。这才是 v1.7 narration_cache 的同构兑现。
- **P1 — normalized JSON-safe + 稳定键**:`_normalize_command` 只存 `{command,a,b:str,delta:int}`,杜绝 UUID/对象/集合进 ledger 致 `json.dump` 崩。键含 gap+month+locale。
- **P1 — 引擎分配唯一 beat Event id**(`transition:{gap_key}:{idx}`),**不信模型 id** → finalizer 去重 + SQLite `INSERT OR IGNORE` 不再静默丢 narration。
- **诚实改口**:reproducibility 契约 = **「gap 一旦生成并存档,reload 永不重生成/不变」**(同 v1.7「事件 narration 一旦生成即冻结」),**不是**"任意存档点所有 reload 逐字节同一"(存档前未生成的 gap 仍首次生成,属固有,非 bug)。
- 测试:in-memory cache-HIT 重放(每次都会出 beat 的 generator → 仅调一次、效果不翻倍、多 beat 唯一 id)+ reject reason/frozen cache JSON 往返存活。full suite **1916 pass / 2 skip**。

### M3 codex 复核（第二轮 → 已修 + 一处 push back,2026-06-15）
- **P1 cache HIT 误依赖 generator(已修)**:`if generator is None: return []` 原在 cache 检查之前 → 无 LLM 的 reload 不重放冻结 narration。改为**cache-HIT 检查置于 generator 检查之前**;generator 仅在 MISS 路径需要。测试:reload 后 `generator=None` 仍重放冻结 beat。
- **P1 locale 未按 run 冻结(已修)**:原用全局 `language_manager.current`,语言中途切换会漂移 gap_key。新增 `_run_locale`:优先读持久化 `world.run_config_snapshot["content_locale"]`,回退 language_manager。
- **P0「生成前存档仍分叉」—— push back(诚实保留)**:codex 要求"任意存档点逐字节确定",但这**与 v1.7 narration 同构且固有**:LLM client 不暴露 seed(v1.7 Q9 正因此选 freeze-on-first-gen)。契约 = **「gap 一旦生成并存档,reload 永不变」**,存档时未生成的 gap 首次到达时新鲜生成 —— 设计如此,非 bug。已加测试**显式记录**该接受契约(reload 走真 timeline、frozen 重放;以及 generator=None 仍重放)。测试改用 patch `scenario_loader.load` 喂回 anchored timeline(load 会从 loader 重建 timeline,旧测试在此处空过)。
- full suite **1917 pass / 2 skip**。M3 经 codex 两轮二审。

### M4（生产 generator 接真 LLM + 有界连贯 context）✅ — L3 引擎闭环成立
- **`make_transition_generator(call_llm_json=...)`**:snapshot-only async generator,把快照组装成 prompt → `call_llm_json` → 解析 `{"beats":[...]}`。**resilient**:任何失败/坏 shape → `[]`(phase 降级,锚点照常)。非 dict beat 丢弃。生产 beat 仍经 phase 的 `validate_beat`(白名单/写集合/边界)→ 模型给的越界 command 被拒,绝不应用。
- **world-free 边界保持**:**phase** 用 `world` 组装 context(它有 world),塞进 snapshot 的 `context_block`;**generator 只读 snapshot**,不碰 world。
- **有界连贯 context(Q4/Q11)**:复用 v1.7 沙箱组装 —— `build_prompt_world_lore`(背景/风格/术语 backbone)+ `_chronicle_context`(近期纪事,已滤 is_story)在 `CONTEXT_BUDGET_CHARS` 内 `_assemble_within_budget`;**`next_anchor_target` 预留**(prompt 全量输出、永不截断 —— codex starvation #10 修复:目的地约束不被挤掉)。authored 字段 `_clip` + fenced 为「参考数据(非指令)」(Q9/Q10)。
- **默认接线**:`attach_default_transition_generator(world)` 在 injector(boot)+ load_game(reload)接上;无 LLM key / 无锚点时无害(phase 仅 gap tick 生成,失败降级)。
- **token 预算 = char 代理**(`CONTEXT_BUDGET_CHARS`,同 v1.7);**staleness**:同步「生成即应用」模型无 plan-ahead 窗口 → staleness 暂不适用(真异步 backfill 才需,出 scope)。
- 测试 `test_scenario_transition_generator.py`:解析 beats、坏 shape/异常→[]、丢非 dict beat、预留 target 在巨 context 下仍在 prompt、**端到端过 phase 应用合法 beat 且拒越界 command**。full suite **1922 pass / 2 skip**。

### M4 codex 二审（FIX-FIRST → 已修,2026-06-16）
codex 通过了关键项(snapshot-only 边界 ✅、command-authority 无绕过 ✅、无循环 import ✅),修了健壮性/契约:
- **P1 无-key gate(已修)**:default generator 原无条件接线、gap tick 必发请求(即便无 key 也向可达 base_url 发匿名请求)。新增 `_llm_available(mode)`(查 `LLMConfig.from_mode().api_key`,fail-closed)+ `make_transition_generator(require_key=True)`:无 key → 跳过生成,绝不发请求。注入 fake 的测试用 `require_key=False`。**额外收益**:现有 scenario 测试 boot 后也不会误触真 LLM。
- **P2 generator 侧防御性净化(已修)**:`build_transition_prompt` 现对**每个**字段(含 `context_block`)`_clip`(_clip 同时**中和 fence 定界符 + 限长**)→ 即便传入任意未净化/超大 snapshot 也不能突破数据区或撑爆 prompt(不再信赖 `_build_snapshot` 上游)。
- **P1→契约校正(target 截断)**:`_next_anchor_target` 组合后仍 `_clip` 到 `TARGET_CAP_CHARS=800`。改口:target **从 context 预算竞争中预留**(单独输出、不被其它层挤掉),但**仍按字段 clip**(同所有 authored 字段),非"绝不截断"。
- **P1→诚实保留(30s 超时)**:`wait_for` 只取消 coroutine,底层请求在 `to_thread` 仍可能跑到 120s —— 这与 v1.7 narrative_fill **同构**。契约 = 「**tick 不会等待超过 30s**」(超时→[]→降级),非「30s 杀死请求」。孤儿线程完成后结果被丢弃,可接受。
- **codex 建议(未做,记 backlog)**:把复用的 v1.7 私有 `_clip/_assemble_within_budget/_chronicle_context` 抽到共享 prompt-sandbox 模块(当前单向依赖无循环,但 v1.7 改动会静默影响 M4)。
- 测试 +4:无-key 不调 provider、prompt 长度有界(任意 snapshot)、fence 中和、默认接线不覆盖已有。full suite **1926 pass / 2 skip**。

### 整体二审（M0-M4 集成复核,FIX-FIRST → 已修,2026-06-16)
逐里程碑审之外,做了一次**集成整体**复核(diff 基线 v1.7 @47c8f41a),挖出单里程碑视角看不到的:
- **P0 间接饿死(已修,关键)**:write-set 非干涉只保护锚点前置闭包,**漏了间接因果**——beat 改 relation R → 某**非 anchor** 事件 B(condition 读 R)触发 → B 的 `blocks_events` 压制 pending 锚点 → 饿死。**修法(更简更强)**:`pending_anchor_read_set` → **`protected_read_set`**:保护**所有** future-pending 事件(anchor 或普通)的 condition 读集合。beat 只写 relation,故只要禁止它碰任何未来事件 condition 读的 token,beat 就**永不能改变哪些事件触发** → 永不影响任何锚点可达性(strictly stronger,且消解了 closure 的遗漏)。移除现已被包含的 `_precondition_closure`/`_activate_storyline_targets`。端到端测试复现 codex 反例(beat→B→blocks→饿死)并验证被拦。
- **P1 编年史挤占(已修)**:`_chronicle_context` 原先**先取 N 条再滤 is_story** → 一串 beat 占满窗口把 authored facts 挤光。改为**超取(N*8)→滤 is_story→截后 N**。
- **P2 冻结键未绑剧本版本(声明限制)**:`_gap_key` 仅含 anchor id+年月+locale;跨剧本版本 reload 旧 cache/relation 可能语义过期。**声明为不支持跨剧本版本加载**(同类存档惯例);需要再加 scenario fingerprint。
- ✅ codex 通过:兄弟字段 var_equals 边界、phase 顺序、cache/cadence/no-key 交互、跨 schema load 对无-anchor 剧本 no-op。注:**内置剧本锚点数全 0 → anchored 集成路径尚无真实内容覆盖**(= 下方"可玩验证"的内容缺口)。
- full suite **1927 pass / 2 skip**。

### 整体二审 复核（第二轮 → 残余 2 项已修,2026-06-16)
- **P0 残余:branch/choice condition(已修)**:`protected_read_set` 原只读 `trigger.condition`,漏了 `branches[].condition` —— branch 事件可 `always` 触发却按 relation 选不同 branch、跑该 branch 的 effects(world_event_trigger/set_flag/activate_storyline)间接改走向。修:protected 现并入 future-pending 事件的 `branches[].condition` + `choices[].condition`。**附带修正**:`condition_read_set(None)` 原返回 PROTECT_ALL(误),改为返回空集 —— 无 condition = 无条件触发 = 与 beat 无关。
- **P1 硬保证:查询层剔除 is_story(已修)**:`N*8` 超取只是启发式,够多 beat 仍能挤光。改为 `get_events`/`get_recent_events` 新增 `exclude_story` 参数,在 **SQL WHERE 层** `e.is_story = FALSE`,`_chronicle_context` 用它 → authored facts 永不被 beat 挤出有界窗口(EventManager 内存路径同样过滤)。
- 测试:branch condition 被保护、`condition_read_set(None)`=空、40 个 is_story 洪水下 chronicle 仍只返回 authored。full suite **1930 pass / 2 skip**。

### 整体二审 复核（第三轮 → mod effect 残余已修,2026-06-16)
- **P0 残余:注册式 mod effect(已修,最终闭合)**:future `always` 事件可跑注册 mod effect(任意 callable)读 relation → 写 flag/triggered → 桥接锚点。protected 只分析 condition 不分析 effect。修:`_has_unanalyzable_effect` —— future-pending 事件若带**非 `CANONICAL_EFFECT_TYPES`** 的 effect(top-level/branch/choice),`protected_read_set` 加 `PROTECT_ALL` fail-closed(该事件 pending 期间拒一切有状态 beat)。canonical effect 应用固定值、不按 relation 分支 → 无需。
- **P0 类彻底闭合**:beat 只写 relation;该 relation 若被任何 future 事件**读**(trigger/branch/choice condition)**或** 任何 future 事件带 mod effect(fail-closed)→ 拒;canonical effect 不读 relation → 不桥接。∴ beat 只能碰"无未来事件在意"的 relation → **可证永不改变哪些事件触发/其 outcome**。
- ✅ P1 查询层 exclude_story 经复核确认硬保证成立。测试:mod effect→PROTECT_ALL、canonical effect 不触发 fail-closed。full suite **1932 pass / 2 skip**。

> **L3 引擎闭环成立**(经逐里程碑 + 整体 + 复核 codex 二审):真 LLM 在 scripted 锚点之间生成过渡 beat,受**全事件 read-set 非干涉**守门(beat 永不改变哪些事件触发)、可复现(冻结缓存)、可降级(无 key 走纯锚点)、安全可证(机制走专属 applier)。**剩下到「可玩验证」= 内容(anchored 剧本)+ 前端 beat 渲染 + Layer3/4 RC 验证**(非引擎)。

## 验收（收敛后,带非空过守门）
- 标 `anchor` 的事件**全部按时触发、触发时前置成立**,无论过渡是否生成(跨锚点定时重放差分,**比完整锚点 ID 集**,真谓词前置,确定性 RNG)。
- 过渡开启时相邻锚点间出现连贯 beat(顺剧本、当前语境如六朝);**effect ∈ 白名单、写集合不与任一 pending 锚点谓词相交**。同测试含一允许 + 一禁止 effect。
- **非空过见证**:差分「开」run 实际调用了 generator、接受 ≥1 beat、应用了非零白名单 effect。
- 违规 beat(试图饿死锚点 / 越白名单)→ **被拒**(断言 call count + 拒因 + 零应用),锚点仍触发。
- reload save 后 beat 序列 + effects + 文本不变,**不重询 LLM**(ledger 确定性 replay;断言 load 后 generator 零调用)。
- no-scenario / 无 LLM key 全链路降级为纯剧本锚点,正常可玩。
- full pytest 零回归;跨 schema(default/liuchao/sanguo)锚点原语可 load + 生效。

---

## codex 二审纪要（2026-06-14,第一轮 —— vet 推荐值 + 三类风险）

**裁决:8 CONCUR / 3 DISSENT。**
- **CONCUR**:Q1(显式 anchor 优于重载)、Q2((b) 是唯一可行的有状态权限级,但须更窄的 transition 校验器)、Q4(批量合理,需 staleness)、Q6(复用有界组装,需预留 + normalized 事实)、Q7(可选、现已优雅降级)、Q9(沙箱可复用)、Q10(注入已证,需扩返回类型)、Q11(键含 locale,需冻结 run locale)。
- **DISSENT(全采纳,改设计)**:
  - **Q3** — 当前真值测试证不了未来可达(锚点到期前合法为假 + 一次性精确月);改 write-set/非干涉 + 定时重放。
  - **Q5** — `Event`/`EventStorage` 无 effects 字段、load 恢复状态不 replay 事件;改独立 normalized beat ledger。
  - **Q8** — 验收测试规格不足;加非空过见证 + 确定 RNG + 完整锚点 ID 集 + 允许/禁止双 effect + 禁 `always`。

**三类风险(codex 挖出,已折入)**:
- **空过陷阱**:M0 差分若无锚点/无 gap/无 generator 则恒绿;违规拒绝 fixture 若 generator 从未被调则恒绿;白名单断言对空序列恒真;`always` 谓词证不了前置;持久化测试可因末态已序列化而恒绿。→ 全部以 Q8 非空过见证 + 完整 ID 集 + 真谓词 + ledger 存在断言堵死。
- **可达性饿死(11 向量)**:exact-month 一次性丢失、relation/flag/var 喂谓词、`world_event_trigger` 追加 triggered ids 使锚点被当已触发、`activate_storyline` 改激活线、同月排序(优先级未落地)、批量计划过期、context 截断丢锚点目标、预算耗尽。→ 写集合非干涉 + `hard_anchor` 优先级 + staleness + 预留 + anchor-reserved 配额。
- **authority 泄漏(8 点)**:可变 world 句柄、通用 effect lane(含 mod callable)、partial rollback 不安全、dispatcher 元数据(id/requires/blocks/storyline/trigger/branches)、`Event.content` 回流、`state` 被 `var_equals` 读、未校验实体引用、handler 直接 `spawn_npc` 旁路。→ snapshot-only + transition 命令 schema + 专属 applier + 文本仅 narration + ledger 非 state + 显式实体解析。

**Q2 最终建议(codex)**:**(b),实现为 transition 命令 schema**,初始排除 `world_event_trigger`/`activate_storyline`/flags/vars/死亡·境界·势力/mod effects/写集合相交项;relation delta 仅当 pair 不在任何 pending 锚点依赖且有界。generator 收不可变快照、返回数据、过 schema/ID/write-set/staleness 校验、prose 仅入 `Event.narration`。(a) 不交付 L3 机制;(c) 不安全(通用 lane + partial rollback + 可变 world + 纯结构校验皆守不住边界)。

*下一步:御主拍最终板(尤其 Q2 白名单具体集 + Q5 ledger schema + M0 闸门范围)→ 锁决策 → 起 M0。本版仅收敛草案,未实现。codex resume: `019ec3fe-0e00-7e00-995e-4754c2038b65`。*
